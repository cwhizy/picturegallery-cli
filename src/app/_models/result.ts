import { MediaUrl } from './mediaurl';

export interface Result {
    id: number,
    title: string,
    image: string,
    num_of_comments: number,
    num_of_shares: number,
    num_of_likes: number
}
